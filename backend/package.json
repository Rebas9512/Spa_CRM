{
  "name": "@spa-crm/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute spa-crm-db --local --file=./src/db/schema.sql",
    "db:seed": "wrangler d1 execute spa-crm-db --local --file=./src/db/seed.sql"
  },
  "dependencies": {
    "@spa-crm/shared": "*",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
